import React from 'react';

const DataError = () => {
    return (
        <div>
            We are sorry :( something went wrong while requesting the data
        </div>
    );
};

export default DataError;